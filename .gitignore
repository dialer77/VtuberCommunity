# Dependencies
node_modules/
.pnp
.pnp.*

# Build outputs
.next/
out/
build/
dist/

# Env / secrets (API 키 등은 절대 커밋 금지)
.env
.env*.local
*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Caches
.turbo/
*.tsbuildinfo
next-env.d.ts

# 자체 DB (SQLite) — 로컬 데이터, 커밋 금지
data/
*.db
*.db-wal
*.db-shm

# 로컬 도구 바이너리 (ngrok 등) — 커밋 금지
tools/

# Flutter/Dart (app/) — flutter create 후 생성물
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
app/android/
app/ios/
app/linux/
app/macos/
app/windows/
app/web/
app/.metadata
app/pubspec.lock

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
