{
  "name": "vmoa-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VMOA 백엔드 — 수집 스케줄러 + 자체 DB(SQLite) + REST API",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "^5"
  },
  "dependencies": {
    "fastify": "^5.10.0",
    "tsx": "^4"
  }
}
